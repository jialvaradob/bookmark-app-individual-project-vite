/* import React from 'react';
import Book from './Book';

const BookmarkedPage = ({ Displayedbooks }) => {
    return (
        <div className="book-library">
            {Displayedbooks.map((book, id) => (
                <Book key={id} book={book} />
            ))}
        </div>
    );
};

export default BookmarkedPage; */