import { createContext } from 'react';

const SheetContext = createContext();

export default SheetContext;

// This calls the createContext component which allows me to use a value across my project components.
